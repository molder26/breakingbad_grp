import React from "react";
import "./Spinner.css";

const Spinner = () => {
    return (<>
        <div className="spinner"></div>
        <h2>Cargando...</h2>
    </>
    );
};

export default Spinner;
